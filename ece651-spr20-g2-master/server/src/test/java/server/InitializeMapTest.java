package server;

import java.util.ArrayList;

import org.junit.jupiter.api.Test;

import shared.*;
/*
public class InitializeMapTest {
  @Test
  public void testInitializeMap2() {
    Gameserver server = new Gameserver();
    server.initializeMap(2);  // 2 players
    Displayer displayer = Displayer.getInstance();
    displayer.setNumOfPlayer(2);
    ArrayList<Territory> map = server.getMap();
    // print out tids and names
    for (Territory t : map) {
      System.out.println("tid: " + t.getTid() + ", name: " + t.getName());
    }
    displayer.displayMap(map);
  }

  @Test
  public void testInitializeMap3() {
    Gameserver server = new Gameserver();
    server.initializeMap(3);  // 3 players
    Displayer displayer = Displayer.getInstance();
    displayer.setNumOfPlayer(3);
    ArrayList<Territory> map = server.getMap();
    // print out tids and names
    for (Territory t : map) {
      System.out.println("tid: " + t.getTid() + ", name: " + t.getName());
    }
    displayer.displayMap(map);
  }

  @Test
  public void testInitializeMap4() {
    Gameserver server = new Gameserver();
    server.initializeMap(4);  // 4 players
    Displayer displayer = Displayer.getInstance();
    displayer.setNumOfPlayer(4);
    ArrayList<Territory> map = server.getMap();
    // print out tids and names
    for (Territory t : map) {
      System.out.println("tid: " + t.getTid() + ", name: " + t.getName());
    }
    displayer.displayMap(map);
  }
  
@Test
  public void testInitializeMap5() {
    Gameserver server = new Gameserver();
    server.initializeMap(5);  // 5 players
    Displayer displayer = Displayer.getInstance();
    displayer.setNumOfPlayer(5);
    ArrayList<Territory> map = server.getMap();
    // print out tids and names
    for (Territory t : map) {
      System.out.println("tid: " + t.getTid() + ", name: " + t.getName());
    }
    displayer.displayMap(map);
  }

}
*/
